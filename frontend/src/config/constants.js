// ── Workspace panels ──────────────────────────────────────────────────────────
export const SIDEBAR_WIDTH_DEFAULT = 240;
export const SIDEBAR_WIDTH_MIN     = 160;
export const SIDEBAR_WIDTH_MAX     = 400;
export const FEED_WIDTH_DEFAULT    = 280;
export const FEED_WIDTH_MIN        = 200;
export const FEED_WIDTH_MAX        = 480;

// ── Pipeline ──────────────────────────────────────────────────────────────────
export const TOTAL_PLANNING_PHASES   = 12;
export const MINUTES_PER_PHASE       = 2.5;
export const SCROLL_APPROVAL_THRESHOLD = 50;

// ── Dashboard ─────────────────────────────────────────────────────────────────
export const DASHBOARD_AUTO_REFRESH_MS = 30_000;
export const DASHBOARD_PAGE_SIZE       = 12;

// ── Discovery ─────────────────────────────────────────────────────────────────
export const DISCOVERY_TIMEOUT_MS    = 5 * 60 * 1000;
export const MAX_DISCOVERY_ANSWERS   = 20;

// ── Admin ─────────────────────────────────────────────────────────────────────
export const ADMIN_ACTIVITY_PAGE_SIZE = 20;

// ── Auth ──────────────────────────────────────────────────────────────────────
export const BIOMETRIC_STORAGE_KEY = 'pp-biometric';
